# `reflect-metadata` must load before any tsyringe-decorated class. The worker
# entry imports it first thing; tests import handlers directly, so preload it
# here for every test run.
[test]
preload = ["reflect-metadata"]
